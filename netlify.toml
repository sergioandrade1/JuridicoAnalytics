# Site estatico: os arquivos ficam na raiz do repositorio e nao ha etapa de
# build. Sem esta configuracao a Netlify tentaria adivinhar um framework.
# Os cabecalhos de cache ficam no arquivo _headers, que vale tanto para o
# deploy automatico quanto para um deploy manual por linha de comando.
[build]
  publish = "."
  command = ""
